- name: Install OpenSRF Websockets
  copy:
    src: "{{repo_base}}/evergreen-universe-rs/target/release/opensrf-websockets"
    dest: "{{eg_install_path}}/bin/opensrf-websockets"
    owner: opensrf
    group: opensrf
    mode: '0700'
  become: true
  become_user: opensrf
