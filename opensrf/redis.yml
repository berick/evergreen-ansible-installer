- name: Disable Redis Persistence
  become: true
  lineinfile: 
    dest: /etc/redis/redis.conf
    regexp: '^#?\s*save ""'
    line: 'save ""'
- name: Restart Redis
  become: true
  systemd: name=redis-server state=restarted
